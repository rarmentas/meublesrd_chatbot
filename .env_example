# Este archivo contiene variables de entorno para uso local.
#   - Windows (PowerShell): Copy-Item .env_example .env
#   - macOS/Linux: cp .env_example .env
#
# Importante:
# - NO subir ".env" al repo (contiene secretos). Este repo ya lo ignora en .gitignore.
# - En Google Colab, el notebook actual usa "Secrets" (icono 🔑) vía `google.colab.userdata`.
# - Los notebooks están configurados para intentar usar variables de este archivo antes que de OS, si no las encuentra, busca en OS.

# --- Requeridas ---
# OpenAI API Key: usada para generar embeddings (p. ej. text-embedding-3-small).
OPENAI_API_KEY= 

# Pinecone API Key: usada para indexar y consultar el vector store.
PINECONE_API_KEY=

# --- Opcionales (observabilidad/tracing) ---
# LangSmith: tracing/monitoring de ejecuciones de LangChain.
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=

